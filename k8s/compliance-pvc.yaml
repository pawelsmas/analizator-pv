apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: compliance-data-pvc
  namespace: pv-optimizer
  labels:
    app: compliance
    component: storage
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard
