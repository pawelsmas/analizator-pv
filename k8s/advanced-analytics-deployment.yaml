apiVersion: apps/v1
kind: Deployment
metadata:
  name: advanced-analytics
  namespace: pv-optimizer
  labels:
    app: advanced-analytics
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: advanced-analytics
  template:
    metadata:
      labels:
        app: advanced-analytics
        tier: backend
    spec:
      containers:
      - name: advanced-analytics
        image: pv-optimizer/advanced-analytics:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8004
          name: http
          protocol: TCP
        env:
        - name: PYTHONUNBUFFERED
          value: "1"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8004
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8004
          initialDelaySeconds: 15
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: advanced-analytics
  namespace: pv-optimizer
  labels:
    app: advanced-analytics
spec:
  type: ClusterIP
  ports:
  - port: 8004
    targetPort: 8004
    protocol: TCP
    name: http
  selector:
    app: advanced-analytics
