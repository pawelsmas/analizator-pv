apiVersion: apps/v1
kind: Deployment
metadata:
  name: economics
  namespace: pv-optimizer
  labels:
    app: economics
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: economics
  template:
    metadata:
      labels:
        app: economics
        tier: backend
    spec:
      containers:
      - name: economics
        image: pv-optimizer/economics:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8003
          name: http
          protocol: TCP
        env:
        - name: PYTHONUNBUFFERED
          value: "1"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8003
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8003
          initialDelaySeconds: 15
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: economics
  namespace: pv-optimizer
  labels:
    app: economics
spec:
  type: ClusterIP
  ports:
  - port: 8003
    targetPort: 8003
    protocol: TCP
    name: http
  selector:
    app: economics
