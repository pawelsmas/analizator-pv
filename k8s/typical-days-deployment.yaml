apiVersion: apps/v1
kind: Deployment
metadata:
  name: typical-days
  namespace: pv-optimizer
  labels:
    app: typical-days
    tier: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: typical-days
  template:
    metadata:
      labels:
        app: typical-days
        tier: backend
    spec:
      containers:
      - name: typical-days
        image: pv-optimizer/typical-days:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8005
          name: http
          protocol: TCP
        env:
        - name: PYTHONUNBUFFERED
          value: "1"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8005
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8005
          initialDelaySeconds: 15
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: typical-days
  namespace: pv-optimizer
  labels:
    app: typical-days
spec:
  type: ClusterIP
  ports:
  - port: 8005
    targetPort: 8005
    protocol: TCP
    name: http
  selector:
    app: typical-days
