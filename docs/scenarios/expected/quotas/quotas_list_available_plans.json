{
  "status": 200,
  "response_shape": {
    "plans": [
      {
        "id": "string",
        "name": "string",
        "limits": {
          "jobs_per_day": "integer",
          "reports_per_day": "integer",
          "shares_total": "integer",
          "storage_mb": "integer",
          "projects_total": "integer"
        }
      }
    ]
  },
  "assertions": [
    "len(response['plans']) >= 3",
    "'free' in [p['id'] for p in response['plans']]",
    "'pro' in [p['id'] for p in response['plans']]",
    "'enterprise' in [p['id'] for p in response['plans']]"
  ]
}
